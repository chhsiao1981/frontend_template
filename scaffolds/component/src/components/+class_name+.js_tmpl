import React, { PureComponent } from 'react'
import Empty from './Empty'


class {{class_name}} extends PureComponent {
  render() {
    const {myId, ...params} = this.props
    if(!myId) return (<Empty />)

    return (
      <div>
        Hello {{class_name}}: {myId}  
      </div>  
    )
  }
}

export default {{class_name}}
